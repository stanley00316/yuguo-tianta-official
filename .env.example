# 指定站台根網址（含 https；勿結尾斜線）。正式上線建議設定，供 sitemap／robots 產生正確網址
# 未設定時會在 Vercel 上用 VERCEL_URL，本機預設為 http://localhost:3000
# NEXT_PUBLIC_SITE_URL=https://你的網域

# 管理員登入（本機可複製為 .env.local 並填值，或執行 npm run setup:admin-env）
# 部署到 Vercel 時請在專案 Settings → Environment Variables 新增同名變數

# 初始管理密碼（ plaintext ）；若已由後台「變更管理密碼」寫入雜湊，登入將以儲存的雜湊為準（此條仍可保留作備援或未遷移前使用）
ADMIN_PASSWORD=

# 簽署工作階段 Cookie 用密鑰（建議至少 32 字元隨機字串）
ADMIN_SESSION_SECRET=

# 選填：Upstash Redis（Vercel 正式環境之「站內留言收件匣」「首頁背景圖」與「線上變更管理密碼」建議設定；自主機房可完全不設，改由 data/ 檔案儲存）
# 用途一：線上「變更管理密碼」寫入雜湊（Vercel 無硬碟時需要）
# 用途二：站內「聯絡我們」留言（Vercel 正式環境需要；自主機房可省略，改寫 data/contact-inbox.jsonl）
# 用途三：首頁背景圖全訪客共用（Vercel 正式環境需要；自主機房可省略，改寫 data/hero-background.json）
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# --- 聯絡我們：選填「額外寄信通知」（表單成功後會寫入 Redis 或 data/contact-inbox.jsonl；不設定亦不影響站內收信）---

# Web3Forms 存取金鑰（https://web3forms.com）— 儲存成功後額外寄信到您綁定信箱
# WEB3FORMS_ACCESS_KEY=

# 或 Resend（https://resend.com）
# RESEND_API_KEY=
# CONTACT_TO_EMAIL=
# CONTACT_FROM_EMAIL=
